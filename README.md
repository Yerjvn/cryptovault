# 🔷 CryptoVault — Криптокошелёк

Современное веб-приложение криптокошелька на React с интеграцией MetaMask для отправки и получения криптовалют.

![React](https://img.shields.io/badge/React-18.2-61DAFB?logo=react)
![Ethers.js](https://img.shields.io/badge/Ethers.js-6.9-3C3C3D?logo=ethereum)
![Vite](https://img.shields.io/badge/Vite-5.0-646CFF?logo=vite)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Использование](#-использование)
- [Поддерживаемые сети](#-поддерживаемые-сети)
- [Структура проекта](#-структура-проекта)
- [API Reference](#-api-reference)
- [Безопасность](#-безопасность)

---

## ✨ Возможности

- 🔐 **Подключение MetaMask** — безопасная авторизация через расширение браузера
- 💰 **Просмотр баланса** — отображение баланса в реальном времени с автообновлением
- 📤 **Отправка транзакций** — перевод криптовалюты на любой адрес
- 🔄 **Переключение сетей** — поддержка множества блокчейн-сетей
- 📜 **История транзакций** — просмотр совершённых переводов
- 🎨 **Современный UI** — кибер-неоновый дизайн с анимациями

---

## 📦 Требования

- **Node.js** 18.0 или выше
- **npm** или **yarn**
- **MetaMask** расширение для браузера

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd wallet
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### 4. Сборка для продакшена

```bash
npm run build
```

Собранные файлы появятся в папке `dist/`

---

## 📖 Использование

### Подключение кошелька

1. Установите расширение [MetaMask](https://metamask.io/download/) в браузере
2. Создайте или импортируйте кошелёк в MetaMask
3. Откройте приложение и нажмите **"Подключить MetaMask"**
4. Подтвердите подключение в всплывающем окне MetaMask

### Переключение сети

1. После подключения нажмите на название сети в правом верхнем углу
2. Выберите нужную сеть из списка
3. Подтвердите переключение в MetaMask
4. Если сеть отсутствует в MetaMask — она будет добавлена автоматически

### Отправка транзакции

1. Введите адрес получателя (формат: `0x...`)
2. Укажите сумму для отправки
3. Нажмите **"Отправить транзакцию"**
4. Подтвердите транзакцию в MetaMask
5. Дождитесь подтверждения в блокчейне

### Получение тестовых токенов

Для тестирования используйте тестовые сети и бесплатные faucets:

| Сеть | Faucet |
|------|--------|
| Sepolia | [sepoliafaucet.com](https://sepoliafaucet.com/) |
| Goerli | [goerlifaucet.com](https://goerlifaucet.com/) |
| Mumbai | [faucet.polygon.technology](https://faucet.polygon.technology/) |

---

## 🌐 Поддерживаемые сети

### Основные сети (Mainnet)

| Сеть | Chain ID | Символ |
|------|----------|--------|
| Ethereum Mainnet | 1 | ETH |
| Polygon Mainnet | 137 | MATIC |
| BSC Mainnet | 56 | BNB |
| Arbitrum One | 42161 | ETH |

### Тестовые сети (Testnet)

| Сеть | Chain ID | Символ |
|------|----------|--------|
| Sepolia | 11155111 | SepoliaETH |
| Goerli | 5 | GoerliETH |
| Mumbai | 80001 | MATIC |
| BSC Testnet | 97 | tBNB |
| Arbitrum Sepolia | 421614 | ETH |

---

## 📁 Структура проекта

```
wallet/
├── public/
│   └── vite.svg                 # Иконка сайта
├── src/
│   ├── components/
│   │   ├── WalletConnect.jsx    # Экран подключения MetaMask
│   │   ├── WalletInfo.jsx       # Информация о кошельке
│   │   ├── SendTransaction.jsx  # Форма отправки транзакции
│   │   ├── TransactionHistory.jsx # История транзакций
│   │   └── NetworkSelector.jsx  # Выбор блокчейн-сети
│   ├── hooks/
│   │   └── useWallet.js         # Хук для работы с MetaMask
│   ├── App.jsx                  # Главный компонент
│   ├── App.css                  # Стили компонентов
│   ├── index.css                # Глобальные стили
│   └── main.jsx                 # Точка входа
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

---

## 🔧 API Reference

### Хук `useWallet`

Основной хук для взаимодействия с MetaMask и блокчейном.

```jsx
import { useWallet } from './hooks/useWallet';

const wallet = useWallet();
```

#### Возвращаемые значения

| Свойство | Тип | Описание |
|----------|-----|----------|
| `account` | `string \| null` | Адрес подключённого кошелька |
| `balance` | `string \| null` | Баланс в нативной валюте |
| `chainId` | `string \| null` | ID текущей сети |
| `networkName` | `string \| null` | Название текущей сети |
| `networkSymbol` | `string` | Символ валюты сети |
| `isConnecting` | `boolean` | Флаг процесса подключения |
| `error` | `string \| null` | Текст ошибки |
| `isMetaMaskInstalled` | `boolean` | Установлен ли MetaMask |
| `networks` | `object` | Список поддерживаемых сетей |

#### Методы

##### `connectWallet()`

Запрашивает подключение кошелька через MetaMask.

```jsx
const { connectWallet } = useWallet();

await connectWallet();
```

##### `disconnectWallet()`

Отключает текущий кошелёк (локально).

```jsx
const { disconnectWallet } = useWallet();

disconnectWallet();
```

##### `sendTransaction(to, amount)`

Отправляет транзакцию на указанный адрес.

```jsx
const { sendTransaction } = useWallet();

try {
  const receipt = await sendTransaction(
    '0x742d35Cc6634C0532925a3b844Bc9e7595f...',
    '0.1'
  );
  console.log('Транзакция подтверждена:', receipt.hash);
} catch (error) {
  console.error('Ошибка:', error.message);
}
```

**Параметры:**
- `to` (string) — адрес получателя
- `amount` (string) — сумма в нативной валюте

**Возвращает:** `Promise<TransactionReceipt>`

##### `switchNetwork(chainId)`

Переключает MetaMask на указанную сеть.

```jsx
const { switchNetwork } = useWallet();

// Переключиться на Sepolia
await switchNetwork('11155111');
```

**Параметры:**
- `chainId` (string) — ID сети (десятичное число)

##### `refreshBalance()`

Принудительно обновляет баланс кошелька.

```jsx
const { refreshBalance } = useWallet();

refreshBalance();
```

---

## 🔒 Безопасность

### Принципы безопасности

1. **Приватные ключи никогда не покидают MetaMask** — приложение не имеет доступа к вашим ключам
2. **Все транзакции подтверждаются пользователем** — MetaMask показывает детали перед подписью
3. **Нет серверной части** — приложение полностью клиентское
4. **Открытый исходный код** — вы можете проверить весь код

### Рекомендации

- ⚠️ Всегда проверяйте адрес получателя перед отправкой
- ⚠️ Используйте тестовые сети для экспериментов
- ⚠️ Никогда не делитесь seed-фразой или приватными ключами
- ⚠️ Проверяйте URL сайта перед подключением кошелька

---

## 🛠 Технологии

- **React 18** — UI библиотека
- **Vite 5** — сборщик и dev-сервер
- **ethers.js 6** — библиотека для работы с Ethereum
- **CSS Variables** — темизация и стилизация

---

## 📝 Лицензия

MIT License

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

<p align="center">
  Сделано с ❤️ используя React и ethers.js
</p>

